// Short directives
import 'package:mix/src/directives/text_directives/text_directive.utils.dart';

const capitalize = TextDirectiveUtils.capitalize;
const upperCase = TextDirectiveUtils.upperCase;
const lowerCase = TextDirectiveUtils.lowerCase;
const titleCase = TextDirectiveUtils.titleCase;
const sentenceCase = TextDirectiveUtils.sentenceCase;

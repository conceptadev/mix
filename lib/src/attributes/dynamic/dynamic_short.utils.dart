import 'package:mix/src/attributes/dynamic/dynamic.utils.dart';
import 'package:mix/src/attributes/helpers/helper.utils.dart';

/// Dynamic utilities
const xsmall = WrapFunction(DynamicUtils.xsmall);
const small = WrapFunction(DynamicUtils.small);
const medium = WrapFunction(DynamicUtils.medium);
const large = WrapFunction(DynamicUtils.large);

const portrait = WrapFunction(DynamicUtils.portrait);
const landscape = WrapFunction(DynamicUtils.landscape);

const dark = WrapFunction(DynamicUtils.dark);

const variant = DynamicUtils.variant;

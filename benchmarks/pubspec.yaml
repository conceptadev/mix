name: benchmarks
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  mix:
  mockito: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0
  test: ^1.16.0

dependency_overrides:
  mix:
    path: ../

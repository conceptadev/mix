A list of benchmarks to measure the different aspects of Mix

To run a benchmark, run:

```sh
flutter run --release -t lib/some_benchmark.dart
```

#### Resources

* <https://github.com/flutter/flutter/tree/master/dev/benchmarks>
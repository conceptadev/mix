# yaml-language-server: $schema=./schemas/melos.yaml.schema.json
name: mix_workspace

packages:
  - packages/*
  - packages/mix/example
  - packages/mix_generator/example
  - packages/mix_winds/example
  - packages/mix_chart/example
  - packages/*/demo

command:
  bootstrap:
    environment:
      sdk: ">=3.11.0 <4.0.0"
      flutter: ">=3.41.0"
    dependencies:
      meta: ^1.15.0
    dev_dependencies:
      flutter_lints: ^6.0.0
      dart_code_metrics_presets: ^2.24.0
      build_runner: ^2.11.0
  publish:
    hooks:
      pre: melos run gen:build

categories:
  flutter_projects:
    - packages/mix
    - packages/mix_protocol
    - packages/mix_chart_protocol
    - packages/mix_winds
    - packages/mix_winds/example
    - packages/mix_chart
    - packages/mix_chart/example
    - packages/mix/example
    - packages/mix_lint_test
    - packages/annotations
  dart_projects:
    - packages/mix_core
    - packages/mix_generator
    - packages/mix_lint

scripts:
  # ANALYSIS
  analyze:
    run: melos run schema:inventory --no-select && melos run analyze:dart --no-select && melos run analyze:dcm --no-select
    description: Run all static analysis checks.

  schema:inventory:
    run: melos exec --scope="mix_protocol" -- dart run tool/inventory_check.dart --check && melos exec --scope="mix_chart_protocol" -- dart run tool/inventory_check.dart
    description: Verify protocol vocabulary inventory coverage decisions.

  analyze:dart:
    run: melos exec -c 4 -- dart analyze .
    description: Run Dart static analysis checks.

  analyze:dcm:
    run: melos exec -c 4 --depends-on="dart_code_metrics_presets" -- dcm analyze . --fatal-style --fatal-warnings
    description: Run DCM static analysis checks.

  format:
    run: melos exec -c 4 -- dart format . && dart format scripts
    description: Format Dart files in all packages and root scripts.

  format:check:
    run: melos exec -c 4 -- dart format . --set-exit-if-changed && dart format scripts --set-exit-if-changed
    description: Check Dart formatting in all packages and root scripts.

  lint:fix:all:
    run: melos run lint:dart:fix && melos run lint:dcm:fix && melos run format
    description: Apply Dart and DCM fixes, then format all Dart files.

  lint:dart:fix:
    run: melos exec -- dart fix --apply .
    description: Apply Dart analysis fixes.

  lint:dcm:fix:
    run: melos exec --depends-on="dart_code_metrics_presets" -- dcm fix .
    description: Apply DCM fixes.

  gen:watch:
    run: melos exec --order-dependents -- dart run build_runner watch --delete-conflicting-outputs
    description: Generate code for all packages
    packageFilters:
      dependsOn: "build_runner"

  gen:build:
    run: melos run gen:clean && melos run gen:build:flutter && melos run gen:build:dart && melos run gen:winds-registry
    description: Generate code for all packages

  gen:winds-registry:
    run: melos exec --scope="mix_winds" -- dart run tool/gen_registry.dart
    description: Generate the deterministic mix_winds parser registry

  gen:build:flutter:
    run: melos exec --category=flutter_projects --order-dependents -- flutter pub run build_runner build --delete-conflicting-outputs
    description: Generate code for Flutter packages
    packageFilters:
      dependsOn: "build_runner"

  gen:build:dart:
    run: melos exec --category=dart_projects --order-dependents -- dart run build_runner build --delete-conflicting-outputs
    description: Generate code for Dart packages
    packageFilters:
      dependsOn: "build_runner"

  gen:clean:
    run: melos run gen:clean:flutter && melos run gen:clean:dart
    description: Clean generated code for all packages

  gen:clean:flutter:
    run: melos exec --category=flutter_projects --order-dependents -- flutter pub run build_runner clean
    description: Clean generated code for Flutter packages
    packageFilters:
      dependsOn: "build_runner"

  gen:clean:dart:
    run: melos exec --category=dart_projects --order-dependents -- dart run build_runner clean
    description: Clean generated code for Dart packages
    packageFilters:
      dependsOn: "build_runner"

  test:flutter:
    run: melos exec --category=flutter_projects -- flutter test 
    description: Run flutter test
    packageFilters:
      dirExists: test

  test:dart:
    run: melos exec --category=dart_projects -- dart test
    description: Run dart test
    packageFilters:
      dirExists: test

  test:coverage:
    run: melos exec --category=flutter_projects -- flutter test --coverage
    description: Run flutter test with coverage
    packageFilters:
      dirExists: test

  ci:
    run: melos run test:flutter --no-select && melos run test:dart --no-select
    description: Run flutter and dart tests

  brb:
    run: melos run gen:build
    
  brbc: 
    run: melos run gen:clean
  fix:
    run: melos run lint:fix:all
    description: Apply Dart fix, DCM fix, and Dart formatting.
  custom_lint_analyze:
    run: melos exec --depends-on="mix_lint" dart pub run custom_lint

  exports:
    run: melos exec --scope="mix" -- "dart run ../../scripts/exports.dart . && dart format lib/mix.dart"
    description: Generate exports for the mix package

  verify_version_pubspec_changelog:
    run: ./scripts/verify_changelogs.sh packages
    description: Verify that all packages have the same version

  # API compatibility checking with dart_apitool
  api-check:
    run: dart scripts/api_check.dart
    description: "Check API compatibility for mix packages (usage: melos run api-check -- [package] [version])"

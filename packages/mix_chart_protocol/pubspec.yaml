name: mix_chart_protocol
description: mix_protocol vocabulary for Mix chart stylers.
version: 0.0.1-dev.0
publish_to: none
repository: https://github.com/btwld/mix/tree/main/packages/mix_chart_protocol

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  mix:
    path: ../mix
  mix_chart:
    path: ../mix_chart
  mix_protocol:
    path: ../mix_protocol

dev_dependencies:
  analyzer: '>=9.0.0 <11.0.0'
  dart_code_metrics_presets: ^2.24.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

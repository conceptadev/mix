name: mix_protocol
description: Versioned JSON protocol for representable Mix styles and themes.
version: 1.0.0
publish_to: none
repository: https://github.com/btwld/mix/tree/main/packages/mix_protocol

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  ack: ^1.2.0
  flutter:
    sdk: flutter
  mix: ^2.2.0-beta.5

dev_dependencies:
  analyzer: '>=9.0.0 <11.0.0'
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

name: mix_core
description: Platform-agnostic styling engine primitives for Mix. Pure Dart — no Flutter dependency.
version: 0.1.0-dev.0
repository: https://github.com/btwld/mix/tree/main/packages/mix_core

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.24.4
  lints: ^6.0.0

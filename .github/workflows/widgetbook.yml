name: Widgetbook (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Widgetbook workflow is intentionally disabled."
